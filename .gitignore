# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Environment
.env

# Trained model artifacts (reproduce with `make train`)
# Anchored to repo root so it doesn't also ignore src/foryou/db/models.
/models/

# uv
# (uv.lock is committed on purpose)

# OS
.DS_Store
